# 2026-09-02 工作日志

## Zeabur 部署修复（bowen.chuanglut.com）
- 站点托管：Zeabur（Aliyun Beijing 2C 4GB），域名 bowen.chuanglut.com，绑定 GitHub 仓库 zhouliujunteng/shizi-bowen 自动部署
- 故障链：推 GitHub → Zeabur 从仓库根目录构建，前端在 frontend/ 子目录，根目录无 package.json → 构建失败 404；加根级 Dockerfile 后容器运行但 502（Zeabur 要求应用监听 **8080**，nginx 默认 80）
- 最终方案（commit 919d943 + 5192bd1）：根级 **Dockerfile**（node:20-alpine 构建 frontend/ → nginx:alpine 托管 dist）+ **nginx.conf**（listen **8080**，SPA try_files 回退，/assets 30d 缓存，/hanzi-data 7d 缓存）
- 验证通过：首页 200（标题 博闻幼儿园）、/hanzi-data/*.json 200、/login SPA 回退 200
- ⚠️ 经验：Zeabur Docker 服务必须监听 8080；Zeabur 优先使用仓库根 Dockerfile
