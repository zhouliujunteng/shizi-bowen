# 2026-09-19 工作日志

## 字库课件三大升级（commit 6a93e1a 已上线）

### 1. 多音字
- char_courseware 新字段：`variants` JSONB `[{pinyin, stroke_groups, explanation}]`（每个读音独立一套笔画分组+讲解）；主读音仍用原 explanation/stroke_groups 字段
- Literacy 编辑器顶部读音页签 +「添加多音字」；CoursewareCard 多读音 tab 切换（>1 个读音才显示）

### 2. 配图上传
- char_courseware 新字段：`image` STRING 存 base64（前端 canvas 压缩到 800px JPEG 0.8，约 100KB，无需文件存储服务）
- 编辑器左侧配图区上传/移除；CoursewareCard 展示 + 下载 PNG 时合成进图片

### 3. 素材员/审核员体系
- char_courseware 新字段：`review_status`（草稿/待审核/已通过）；注意表里原有 `status` 字段（启用/停用）别混用
- 字库格子徽标：绿=已通过 黄=待审核 灰=草稿/未配置
- 角色权限（前端门控）：素材员=编辑+提交审核；审核员=只读+通过/退回；平台管理员=全部
- 平台端新页面「素材账号」/platform/accounts（手机号+姓名+默认密码+身份），复用 createStaffWithAccount（kindergarten_id 传 null）
- 登录分流：素材员/审核员 → /platform/literacy；PlatformLayout 只显示「字库课件」菜单（标题改 素材工作台）；router 子路由按 meta.roles 限制

### 4. 编辑/审核留痕（commit e071c77）
- char_courseware 再加 `edited_by`/`reviewed_by` STRING（存操作人姓名）
- 保存（草稿/提交审核）记 edited_by=当前用户并清空 reviewed_by（前次审核作废）；审核记 reviewed_by
- 展示：字格下方小字「编·张三 / 审·李四」+ 编辑器 footer 状态行
- 已实测：明 格子显示 已通过 + 编·周刘俊腾 + 审·周刘俊腾

### 5. 素材账号细节（commit f6d7f23）
- 素材员/审核员视角：左侧菜单只剩「字库课件」（此前已实现，本次确认）；修改密码入口在右上角对所有角色可用（短信验证码通道）
- 添加账号弹窗加「随机生成」按钮：3字母+4数字+3字母（去除易混淆字符），填入后可改

### 6. 素材账号可编辑（commit db7ef87）
- 列表每行加「编辑」：可改姓名/身份（素材员↔审核员），手机号只读（登录账号不可改）；复用 saveStaff 通用更新

### 7. 字库大更新：稳健版 800 字（commit a715f7c + 1b2204f）
- 数据源：用户提供的《全速学习_幼儿园两年识字课程项目包.xlsx》（微信收到），「建议教学序列」表中 `3课时版=纳入` 即稳健版 800 字（A启蒙300+B核心500），按「建议教学序」排序；源数据备份 curriculum/稳健版800字.json（含拼音/部首/部件拆分/教学方法/词例/教学序）
- 入库：汉字 821（新增 706 + 更新 94 对齐拼音/拆分/教法/教学序，旧 21 字保留）；偏旁 185（新增 128，含 ⺮⺼㔾 等变体部首）；字段映射 meaning=词例3个、components_note=部件拆分(整体字形则留空)、teaching_tip=教学方法、sort_order=建议教学序
- 笔画数据：hanzi-writer-data 全覆盖，新增 700 个 JSON → hanzi-data 共 885 个文件
- 字库 Excel 还有 3500 全量字库/推荐版1200/强化版1600/两年76周计划/常用词例库 可用，后续升级字库或排课计划可再取

### 8. Zeabur 502 事故（14:10 已恢复）
- 推送字库更新后站点 502 超 40 分钟（推测 Zeabur 侧部署失败且旧容器被回收，非代码问题——本地构建通过、产物仅 5M）
- 恢复方法：`git commit --allow-empty` 空提交推送触发重新部署，4 分钟后 200；已验证新字笔画文件（扶.json）线上可访问
- 经验：Zeabur 502 且代码无辜时，先空提交重触发部署，无效再查控制台日志/端口

### 9. 素材任务领取（commit 4f5aad7 已上线）
- 新表 `task_claim`（素材任务）：user_id/user_name/item_id/category（部首/字根/汉字）
- Literacy 顶部任务条：素材员按类别领取，每次 20 个（取该类别未通过且未被领的，按 sort_order）；显示「我的任务 N 个·已通过 M 个」
- 锁定：被他人领取且未通过的字 → 灰斜纹底+🔒+领取人，素材员点开被拦并提示；自己领的 → 蓝底「我的任务」；已通过自动解锁显示
- 管理员：编辑器 footer 显示领取人 +「释放任务」解锁
- API：fetchTaskClaims/claimTasks/releaseTaskClaim

### 经验
- ⚠️ agent-browser 对 el-dialog footer 按钮的 click 经常不落地（body 内正常），改用 `agent-browser eval "[...document.querySelectorAll('.el-dialog__footer button')].find(b=>b.textContent.includes('X'))?.click()"` 兜底
- 全链路已实测：添加多音字→提交审核（黄）→通过（绿）→清理测试变体；明课件现为 已通过 状态
- schema 新增字段流程：CLI 2.6.1 ADD_FIELDS_AND_RELATIONS（tableDisplayName 必须是「汉字课件」不是「字库课件」）→ sync-backend → 运行时立即可用
