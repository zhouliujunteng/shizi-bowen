# 2026-09-01 工作日志

## 项目推送到 GitHub
- 仓库：https://github.com/zhouliujunteng/shizi-bowen.git（main 分支，首次提交 234 个文件，commit 8021edf）
- 本地已 git init + .gitignore（排除 node_modules / frontend/dist / .env / *.log / .DS_Store）
- ⚠️ 用户的 id_ed25519（juntengworld@foxmail.com）是 tianqi-gongxiu 仓库的 **deploy key**，不能用于其他仓库（推送报 Permission denied to deploy key）
- 解决方案：新生成专用密钥 ~/.ssh/shizi_bowen_deploy（.pub），用户已将其添加为**账号级 SSH key**（github.com/settings/ssh/new）；~/.ssh/config 配置 Host 别名 `github-shizi-bowen`（ssh.github.com:443，IdentitiesOnly yes）
- 远程地址：origin = git@github-shizi-bowen:zhouliujunteng/shizi-bowen.git（今后 push/pull 直接用 origin 即可）
- ⚠️ .workbuddy/memory/ 含管理员密码等敏感信息，已随提交推送；需确认仓库为 Private
