# 2026-09-07 工作日志

## 课后记录弹窗空白修复（commit 969c392 + 360c52b）
- 根因1（代码 bug）：Records.vue openFill 把学员映射成扁平对象 `{id, child_id, name, ...}`，模板却读 `s.child.name` → TypeError 导致 el-dialog 整体渲染失败（标题在、内容空白）。修复：模板改 `s.name`
- 根因2（部署缓存）：修复推送后用户端仍加载旧包——nginx 对 HTML 未设缓存头，浏览器缓存旧 index.html → 引用旧 immutable JS。修复：nginx.conf `location /` 加 `Cache-Control: no-cache, must-revalidate`（/assets 保持 30d immutable 不变）
- 排查方法（可复用）：agent-browser 实测 + `agent-browser console` 拿 JS 报错 + curl 主包定位报错行列（注意 line:col 可能落在 Vue 内部 render 函数，需结合 chunk 哈希比对判断线上版本）；`performance.getEntriesByType('resource')` 看浏览器实际加载的 chunk 哈希 vs `ls dist/assets` 本地哈希，不一致=部署未完成或浏览器缓存旧 HTML；带 `?v=xxx` 查询参数可绕过 HTML 缓存验证
- 顺手清理：孤儿测试排课 id=3（lesson_id 为 null，早期开发残留）
- ⚠️ 残留：临时测试 Zion 账户 19900001111（account_id 1000000000000020）无法通过 GraphQL 注销（deregisterByAccountId 参数名多次尝试不对、delete_account 被 TABLE_ACCESS 禁止、CLI 在本环境 SIGTERM）；其用户资料已删，登录只会落到待开通页，无实际影响；上线前可在 Zion 编辑器手动删除
- agent-browser 已全局安装（npm -g）+ Chrome 152，可用于线上 e2e 复现
