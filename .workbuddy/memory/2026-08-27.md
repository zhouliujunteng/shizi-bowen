# 2026-08-27 工作日志

## Zion 连接器问题诊断（00:30-00:40）
- 现象：本会话所有 zion MCP 调用报 `connect ECONNREFUSED 127.0.0.1:52869`
- 诊断：用户其他任务（其他会话）调用同一 Zion 项目**正常** → 连接器进程活着，但本会话持有**旧的代理端点**（端口 52869 无监听）
- 结论：会话级 MCP 端点缓存失效。**解法：开新对话**（新会话重新初始化 MCP 连接），无需重启应用
- 用户界面 MCP 服务管理显示 zion 绿灯正常，但那是"配置启用"状态，不等于会话端点有效

## ⚠️ 待执行任务清单（新对话接手，按序执行）

### 任务 1：建表 course_package + course_package_item（课程组合）
表结构：
- `course_package`（课程组合）：id / package_name(text) / description(text) / status(text, 默认"启用") / sort_order(int) / created_at(timestamptz)
- `course_package_item`（组合明细）：id / package_id(bigint FK→course_package, on delete cascade) / lesson_id(bigint FK→lesson) / day_of_week(int, 1-7) / start_time(time) / duration_minutes(int, 默认30)
- 关系：course_package 1:N course_package_item（cascade）；lesson 1:N course_package_item
- 流程：schema_session LOAD → database ADD_TABLES → ADD_FIELDS_AND_RELATIONS → VALIDATE → sync_backend
- 前端已就绪：平台端 /platform/packages（Packages.vue），园所端排课页「按组合排课」三步视图

### 任务 2：建表 literacy_item + literacy_composition（识字字库）
表结构：
- `literacy_item`：id / item_type(text: 字根/汉字/词语/短句) / level(int: 0-3) / content(text) / pinyin(text) / meaning(text) / components_note(text) / teaching_tip(text) / sort_order(int) / status(text, 默认"启用")
- `literacy_composition`：id / parent_id(bigint FK→literacy_item) / child_id(bigint FK→literacy_item) / sort_order(int)；unique(parent_id, child_id)
- 数据：读 `/Users/zhouliujunteng/WorkBuddy/博文幼儿园/curriculum/literacy-bank.json`（301 条目 + 377 关系），批量 insert（可用 runtime_run_code，注意 id 映射：JSON 里的 R001/C001/W001/S001 是逻辑 id，入库后要用数据库自增 id 重建关系）

### 任务 3：造演示课程组合数据
2~3 套示例组合（如"识字启蒙周计划"：周一/三/五识字课节；"脑力+英语混合套餐"），用 runtime GraphQL insert

### 任务 4：实测批量排课语法
验证 `insert_schedule(objects: $objects)` 每条 object 带 `students: { data: [...] }` 的批量嵌套插入（前端 batchCreateSchedules 已按此写好，Zion 端未实测）

### 任务 5（贯穿）：Zion 行级权限
上线前必做：Logged-in User 角色当前数据表权限全开（裸奔），需配 kindergarten_id 比对实现园所隔离
- 01:30 现状核查：全部表 `hasCustomCondition: false`（确认裸奔）；预定义角色仅 Logged-in User / Anonymous User
- 方案要点：需建自定义角色（平台角色/园所角色）→ 账户绑角色（fz_account_has_permission_role）→ UPDATE_ROLE_TABLE_PERMISSION 配行条件；涉及登录/资料流改造，风险高，待与用户确认后专项执行

## ✅ 执行结果（01:00-01:35，新会话用 zion-mcp CLI 绕开失效代理完成）
- **关键发现**：会话 MCP 代理（127.0.0.1:52869）彻底失效且无法会话内恢复；改用 `npx zion-mcp` CLI 直连。**版本分工：2.6.1 干 schema 编辑（tool-call 正常），2.7.0(latest) 干 runtime 数据（graphql 正常）**；2.7.0 的 schema tool-call 有 bug（报 l3_1），2.6.1 的 runtime graphql 有兼容问题（simplifiedCaughtUpServerSchema）
- 任务1✅：4 表建成并部署（schemaExId 7XNwlo1BeD7）。参数规范：tableDisplayName/tableApiName；字段 displayName/apiName/typeIdentifier/required；**旧类型系统不允许 defaultValue**；**created_at/updated_at/id 内置勿重复加**；无 INT 用 BIGINT，无 TIME 用 TIMETZ；relationType 小写 `one_to_many`（无 many_to_one），方向=一→多，fieldApiNameInTargetTable 传关系名（如 package），FK 列自动=关系名+_id
- 任务2✅：literacy_item 301 条 + literacy_composition 377 条入库（(level,sort_order) 映射逻辑 id→自增 id）
- 任务3✅：3 套演示组合（id 2/3/4，共 10 明细）。⚠️ 嵌套插入 course_package→items 被权限拒绝，拆两步直插成功；前端 Packages.vue 本来就是两步写法，无影响
- 任务4✅：`insert_schedule(objects)` + `students:{data:[...]}` 批量嵌套插入**以真实用户 JWT 实测通过**（schedule id 6/7 各带 3 学员）。注意：CLI admin 身份（user 9999999999999）嵌套插入会被 TABLE_ACCESS 拒绝，是 CLI 特有怪癖，真实用户 JWT 正常
- 登录实测：authenticateWithUsername(18588258585/bowen2026admin) 正常签发 JWT

## 字库课件系统（02:00-02:20 完成）
- 需求：平台管理员给每个字配专属讲解 + 任意笔画分组注释（颜色区分）→ 老师课节里看课件、下载图片
- 拍板：新建 char_courseware 表（item_id 1:1 识字条目，explanation TEXT + stroke_groups JSONB）；课件入口=课表页「课件」按钮
- 笔画数据：hanzi-writer-data，155 字（L0+L1）JSON 抽到 frontend/public/hanzi-data/ 按需加载；**没用 hanzi-writer 库，直接渲染 SVG path**（y 轴翻转 transform="scale(1,-1) translate(0,-900)"），编辑/着色/下载全自控
- 文件：components/CharStrokes.vue（笔画 SVG，编辑点选/查看复用）、components/CoursewareCard.vue（课件卡+笔顺演示+SVG→canvas→PNG 下载）、views/platform/Literacy.vue（字库管理+编辑器，路由 /platform/literacy）、Schedules.vue 加「课件」列
- stroke_groups 结构：[{label, color, strokes:[笔画序号]}]，一笔只属一组，未分组笔画显示黑/灰
- 演示数据：「明」(item_id 41) 已配课件（日橙+月蓝+讲解），courseware id 1
- 已验证：build 通过；真实 JWT 查 155 条含课件关联正常
- 待办候选：课节↔汉字绑定（当前老师手动选字）；Anonymous/Logged-in 权限暂缓

## Bug 修复（02:20）：课件编辑器字形空白
- 现象：字点开左侧空白，无法点选笔画
- 根因：CharStrokes.vue 用了 `<script>+<script setup>` 双块共享缓存变量的写法，组件静默失败
- 修复：缓存抽到 src/utils/hanzi.js（loadHanziChar），组件改单 script setup，加加载中/兜底态
- 浏览器全流程实测通过：朋字渲染→新增分组→点 4 笔→填注释→保存→落库（朋 课件 id 2，测试数据用户可重配）

## 业务流程细化（02:30-03:00 + 09:35 修复）
- **字库扩充**：57 偏旁（level 0, sort 101+）+ 26 字母（level 10）+ 52 单词（level 11，每字母2词），共 135 条入库；偏旁笔画 JSON 已复制到 public/hanzi-data（57/57 全覆盖）
- **lesson_item 课节内容表**：lesson_id/item_id/sort_order/teaching_note，已部署；166 条绑定（识字10课+英语10课）
- **课程重构**：识字 10 课（数字笔画→象形自然/人身/动植物→会意上中下→天气季节→动作），英语 10 Unit（字母A-Z渐进+复习课）；新课节 id：识字10-16、英语17-23
- **平台端新页**：/platform/courses 课程内容管理（课节增删改、上下移、内容绑定搜索添加排序）；Literacy.vue 改 5 类标签（汉字/字根/偏旁/字母/单词）
- **顺序解锁（分组独立进度）**：进度=schedule 状态已完成 推导；排课课节下拉只可选「已完成(复习)+下一节」且自动选中下一节；课件弹窗改为只显示本节课节绑定内容 + 已完成课节复习切换（自由搜索已移除）
- **Bug 修复（09:35）**：长英文单词（ice cream/umbrella）课件显示溢出 → CharStrokes 兜底文字按长度自适应字号+换行+overflow hidden；字母/单词编辑器隐藏无意义的笔画分组面板

## 账号体系清理 + 修改密码（10:30-11:10）
- **清理**：删测试员工 user#2/#3（18600000002/3）+ 其 Zion 账户（deregisterByAccountId）；删测试排课 id 6/7（含学员）、朋测试课件 id 2、临时账户 18900000001。园所员工现为空，需用户录真实员工
- **正式管理员**：18588258585 旧账户注销重建（authenticateWithUsername register），**新密码 `Bw#2026JunTeng!`**（旧密码 bowen2026admin 已失效），新 account_id=1000000000000006 已回绑 user#1
- **关键发现**：Zion 密码修改只能走验证码通道——`sendVerificationCodeToPhone(tel, RESET_PASSWORD)` + `resetPasswordWithPhoneNumberVerificationCode(code, phoneNumber, newPassword)`（返回 FZ_Account）；直接 update_account.password 无效（认证走独立凭证存储）；`createPredefinedAccount` 建的账户 authenticateWithUsername 不认（那是 loginWithPassword 体系，勿混用）；短信发送已测有额度；GraphQL 内省对匿名/用户关闭，但 runtime graphql admin 通道可用
- **前端**：components/ChangePasswordDialog.vue（手机号只读+验证码60s倒计时+新密码确认），两端 Layout 头部加「修改密码」；auth.js 加 sendResetPasswordCode/resetPasswordByCode
- 坑：el-input v-model 绑只读 prop 编译报错，用 :model-value
- **待用户实测**：修改密码的短信全流程（验证码发到他真实手机），员工自助注册后改密同理

## 账号体系改为层级开户（2026-08-28 09:30）
- 决策：删除自助注册；平台管理员创建园长账号、园长创建老师账号
- Login.vue 注册入口删除（提示"账号由管理员统一开通"）
- auth.js 加 registerAccount（代注册，不覆盖当前登录态 JWT）；data.js 加 createStaffWithAccount（注册账户+建/绑用户资料一次完成；手机号已有账号报 ACCOUNT_EXISTS）
- Staff.vue：添加员工表单加「初始密码」，开户即用；平台端 Kindergartens.vue 每园所加「园长账号」按钮
- 浏览器全流程实测通过：王园长登录→添加李老师(18600000004)→李老师直接登录成功（李老师留作第二个老师测试账号，密码同 Bowen2026shizi）
- 测试账号现状：18588258585 平台管理员 / 18600000002 王园长 / 18600000003 陈老师 / 18600000004 李老师（密码均 Bowen2026shizi）
